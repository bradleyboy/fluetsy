{
  "name": "fluetsy",
  "version": "0.0.1",
  "description": "Fluent, promise-based library for accessing the Etsy API.",
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "babel lib --out-dir dist",
    "lint": "eslint lib",
    "test": "mocha --watch --compilers js:babel/register --recursive"
  },
  "keywords": [
    "etsy",
    "api",
    "promise",
    "es6"
  ],
  "author": "Brad Daily <brad@bradleyboy.com>",
  "license": "MIT",
  "dependencies": {
    "jsonp-promise": "^0.1.2",
    "node-fetch": "^1.3.0"
  },
  "devDependencies": {
    "babel": "^5.5.8",
    "mocha": "^2.2.5"
  }
}
